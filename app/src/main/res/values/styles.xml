<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/statusbar</item>
        <item name="colorAccent">@color/statusbar</item>

    </style>
    <style name="SalardNaviHeader">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">#ffffff</item>

    </style>
    <style name="SalardNavi">

        <item name="android:textColor">#ff0000</item>
        <item name="android:textSize">16sp</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="android:fitsSystemWindows">true</item>

        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="toolbarStyle">@style/SalardToolbar</item>
    </style>
    <style name="AppTheme.NoStatusBar">
        <item name="android:windowFullscreen">true</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="SalardToolbar" parent="Widget.AppCompat.Toolbar">
        <item name="titleTextAppearance">@style/SalardTitleTextAppearance</item>
        <item name="titleTextColor">@color/SalardColor</item>
        <item name="title">마이 샐러드</item>


    </style>

    <style name="SalardTitleTextAppearance" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="titleTextColor">@color/SalardColor</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" >
        <item name="colorPrimaryDark">@color/statusbar</item>

    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="salardRatingBar" parent="@android:style/Widget.RatingBar">

        <item name="android:progressDrawable">@drawable/salard_ratingbar</item>
        <item name="android:maxHeight">22dp</item>
        <item name="android:minHeight">22dp</item>
    </style>

</resources>
