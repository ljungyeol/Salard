<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:startColor="@color/statusbar"
        android:centerColor="@color/statusbar"
        android:endColor="@color/statusbar"
        android:type="linear" />
</shape>